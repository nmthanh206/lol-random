export type Champion = {
  championName: string;
  image: string;
  id: string;
};

export type Item = {
  itemName: string;
  image: string;
  isBoots: boolean;
  isLegendary: boolean;
  id: string;
};
